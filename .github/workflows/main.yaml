name: Build Executable

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.x

    - name: Install pyinstaller (and UPX for Linux)
      shell: bash
      run: |
        pip install pyinstaller pynacl
        if [[ "${{ matrix.os }}" == "ubuntu-latest" ]]; then
          sudo apt-get update && sudo apt-get install -y upx
        fi
        if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
          pip install windows-curses
        fi

    - name: Set OS-specific name
      shell: bash
      run: |
        if [[ "${{ matrix.os }}" == "ubuntu-latest" ]]; then
          echo "os_name=linux" >> $GITHUB_ENV
        elif [[ "${{ matrix.os }}" == "windows-latest" ]]; then
          echo "os_name=windows" >> $GITHUB_ENV
        elif [[ "${{ matrix.os }}" == "macos-latest" ]]; then
          echo "os_name=macos" >> $GITHUB_ENV
        fi

    - name: Build executable
      shell: bash
      run: |
        if [[ "${{ env.os_name }}" == "linux" ]]; then
          if command -v upx >/dev/null 2>&1; then
            UPX_PATH=$(which upx)
            pyinstaller --onefile --add-data "res/*:res" --hidden-import=cffi --hidden-import=nacl.signing --collect-binaries=nacl --strip --upx-dir "$UPX_PATH" --name lentbuddy bootstrap.py
          else
            pyinstaller --onefile --add-data "res/*:res" --hidden-import=cffi --hidden-import=nacl.signing --collect-binaries=nacl --strip --name lentbuddy bootstrap.py
          fi
        else
          pyinstaller --onefile --add-data "res/*:res" --hidden-import=cffi --hidden-import=nacl.signing --collect-binaries=nacl --name lentbuddy bootstrap.py
        fi
      
    - name: Rename executable per OS
      shell: bash
      run: |
        mkdir output
        if [[ "${{ env.os_name }}" == "windows" ]]; then
          cp dist/lentbuddy.exe output/lentbuddy.exe
        else
          cp dist/lentbuddy output/lentbuddy
        fi

    - name: Upload raw artifact
      uses: actions/upload-artifact@v4
      with:
        name: lentbuddy-${{ env.os_name }}
        path: output/*

  package:
    needs: build
    runs-on: ubuntu-latest

    steps:

    - name: Checkout code
      uses: actions/checkout@v3
  
    - name: Download all build artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    # Read INSTALL_DIR for Linux
    - name: Read Linux INSTALL_DIR
      id: read_linux_dir
      shell: bash
      run: |
        INSTALL_DIR=$(cat res/INSTALL_DIR_LINUX.txt)
        echo "INSTALL_DIR=$INSTALL_DIR" >> $GITHUB_ENV

    - name: Package Linux
      shell: bash
      run: |
        mkdir -p pkg/linux/lentbuddy
        cp artifacts/lentbuddy-linux/lentbuddy pkg/linux/lentbuddy/lentbuddy

        cat << EOF > pkg/linux/lentbuddy/install.sh
        #!/usr/bin/env bash
        set -e
        echo "Installing lentbuddy..."
        install -m 755 lentbuddy "$INSTALL_DIR/lentbuddy"
        echo ""
        echo "Done! Make sure $INSTALL_DIR is in your PATH."
        read -n 1 -s -r -p "Press any key to exit..."
        EOF
        chmod +x pkg/linux/lentbuddy/install.sh

        cat << EOF > pkg/linux/lentbuddy/README.txt
        lentbuddy Installer
        -------------------
        Installs lentbuddy into $INSTALL_DIR.
        Make sure this directory is in your PATH.
        Review this installer before running.
        Press any key at the end to close.
        EOF

        cd pkg/linux
        zip -r ../../lentbuddy-linux.zip lentbuddy

    # Read INSTALL_DIR for macOS
    - name: Read macOS INSTALL_DIR
      id: read_macos_dir
      shell: bash
      run: |
        INSTALL_DIR=$(cat res/INSTALL_DIR_MACOS.txt)
        echo "INSTALL_DIR=$INSTALL_DIR" >> $GITHUB_ENV

    - name: Package macOS
      shell: bash
      run: |
        mkdir -p pkg/macos/lentbuddy
        cp artifacts/lentbuddy-macos/lentbuddy pkg/macos/lentbuddy/lentbuddy

        cat << EOF > pkg/macos/lentbuddy/install.sh
        #!/usr/bin/env bash
        set -e
        echo "Installing lentbuddy..."
        install -m 755 lentbuddy "$INSTALL_DIR/lentbuddy"
        echo ""
        echo "Done! Make sure $INSTALL_DIR is in your PATH."
        read -n 1 -s -r -p "Press any key to exit..."
        EOF
        chmod +x pkg/macos/lentbuddy/install.sh

        cat << EOF > pkg/macos/lentbuddy/README.txt
        lentbuddy Installer
        -------------------
        Installs lentbuddy into $INSTALL_DIR.
        Make sure this directory is in your PATH.
        Review this installer before running.
        Press any key at the end to close.
        EOF

        cd pkg/macos
        zip -r ../../lentbuddy-macos.zip lentbuddy

    # Read INSTALL_DIR for Windows
    - name: Read Windows INSTALL_DIR
      id: read_windows_dir
      shell: bash
      run: |
        INSTALL_DIR=$(cat res/INSTALL_DIR_WINDOWS.txt)
        echo "INSTALL_DIR=$INSTALL_DIR" >> $GITHUB_ENV

    - name: Package Windows
      shell: bash
      run: |
        mkdir -p pkg/windows/lentbuddy
        cp artifacts/lentbuddy-windows/lentbuddy.exe pkg/windows/lentbuddy/lentbuddy.exe

        # Directly embed INSTALL_DIR value into the batch script
        cat << EOF > pkg/windows/lentbuddy/install.bat
        @echo off
        set INSTALL_DIR=$INSTALL_DIR

        echo Installing lentbuddy...
        if not exist "%INSTALL_DIR%" mkdir "%INSTALL_DIR%"
        copy /Y lentbuddy.exe "%INSTALL_DIR%\lentbuddy.exe" >nul

        :: Add to PATH only if not already present
        echo %PATH% | find /I "%INSTALL_DIR%" >nul
        if errorlevel 1 (
            setx PATH "%PATH%;%INSTALL_DIR%"
            echo Added to PATH.
        ) else (
            echo Already in PATH.
        )

        echo.
        echo Done! Restart your terminal.
        pause
        EOF

        cat << EOF > pkg/windows/lentbuddy/README.txt
        lentbuddy Installer
        -------------------
        Installs lentbuddy into $INSTALL_DIR.
        Review this installer before running.
        EOF

        cd pkg/windows
        zip -r ../../lentbuddy-windows.zip lentbuddy

    # -------- Signing step --------
    - name: Set up Python for signing
      uses: actions/setup-python@v4
      with:
        python-version: 3.x

    - name: Install signing dependency
      run: |
        pip install cffi
        pip install pynacl

    - name: Sign artifacts
      shell: python
      env:
        PRIVATE_KEY: ${{ secrets.UPDATE_SIGNING_KEY }}
      run: |
        import os
        from pathlib import Path
        from nacl.signing import SigningKey

        signing_key = SigningKey(bytes.fromhex(os.environ["PRIVATE_KEY"]))

        # Sign zip files
        for zip_file in Path('.').glob('*.zip'):
            with open(zip_file, 'rb') as f:
                data = f.read()
            sig = signing_key.sign(data).signature
            sig_file = zip_file.with_suffix(zip_file.suffix + '.sig')
            with open(sig_file, 'wb') as f:
                f.write(sig)
            print(f"Created signature for {zip_file.name}")

    - name: Upload packaged artifacts + signatures
      uses: actions/upload-artifact@v4
      with:
        name: packaged
        path: |
          *.zip
          *.zip.sig

  release:
    if: github.event_name == 'push'
    needs: package
    runs-on: ubuntu-latest

    steps:
    - name: Download packaged artifacts
      uses: actions/download-artifact@v4
      with:
        name: packaged
        path: release-files

    - name: Get latest release tag
      id: get_latest_release
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        latest_tag=$(gh api repos/${{ github.repository }}/releases/latest --jq .tag_name)
        echo "latest_tag=$latest_tag" >> $GITHUB_ENV

    - name: Upload archives and signatures to release
      uses: svenstaro/upload-release-action@v2
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        tag: ${{ env.latest_tag }}
        file: release-files/*
        file_glob: true
        overwrite: true